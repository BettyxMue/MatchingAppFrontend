import React from "react"
import { KeyboardAvoidingView, ScrollView, Text, View } from 'react-native'

const Topbar = () => {
    return(
        <View style={{width: "100%", height:"10%", alignContent: "center", justifyContent: "center"}}>
            <Text>Finder</Text>
        </View>
    )
}

export default Topbar